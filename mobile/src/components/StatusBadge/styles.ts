import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  badge: { alignSelf: 'flex-start', borderRadius: 20, paddingHorizontal: 10, paddingVertical: 6 },
  label: { fontSize: 11, fontWeight: '700' },
})
